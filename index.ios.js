/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 */

import React, {
  AppRegistry,
  Component,
  StyleSheet,
  Text,
  View
} from 'react-native';

import wrapper from './wrapper'

AppRegistry.registerComponent('Sentiment', () => wrapper);
